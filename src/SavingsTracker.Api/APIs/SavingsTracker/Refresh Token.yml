info:
  name: Refresh Token
  type: http

http:
  method: POST
  url: http://localhost:5075/api/auth/refresh-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
